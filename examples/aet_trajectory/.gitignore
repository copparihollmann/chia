# Generated by run.py. Rebuild any of it from data/*.jsonl with --replay; committing a run
# directory would commit the CLI's own transcripts and the raw profiler trace, which carry
# host paths and worker ids that are not measurements.
runs/
aet_run/
